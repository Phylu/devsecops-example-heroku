on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Run Crashtest Security Scan
    steps:
    - name: Crashtest Security Action Step
      id: crashtest-security
      uses: phylu/crashtest-security-action@0.1.0
      with:
        crashtest-webhook: 'Test'